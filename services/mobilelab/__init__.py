"""Mobile Lab Station services.

This package holds the writer and the synthetic fixture. Both share one record
model and one database layer, so a change to the record shape changes both.
"""

__version__ = "0.1.0"
